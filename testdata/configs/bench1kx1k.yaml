# bench/bench1kx1k.yaml
# Benchmark job: 1k A vs 1k B, no cross-map pre-seeded.

job:
  name: bench_1kx1k
  description: Benchmark — 1k x 1k synthetic data, empty cross-map

datasets:
  a:
    path: testdata/dataset_a_1k.csv
    id_field: entity_id
    format: csv
  b:
    path: testdata/dataset_b_1k.csv
    id_field: counterparty_id
    format: csv

cross_map:
  backend: local
  path: bench/crossmap.csv   # will not exist → empty map
  a_id_field: entity_id
  b_id_field: counterparty_id

embeddings:
  model: all-MiniLM-L6-v2
  a_cache_dir: bench/cache
  b_cache_dir: bench/cache

blocking:
  enabled: true
  operator: and
  fields:
    - field_a: country_code
      field_b: domicile

candidates:
  enabled: false
  field_a: short_name
  field_b: counterparty_name
  method: fuzzy
  scorer: wratio
  n: 10

match_fields:
  - field_a: legal_name
    field_b: counterparty_name
    method: embedding
    weight: 0.55

  - field_a: short_name
    field_b: counterparty_name
    method: embedding
    weight: 0.20

  - field_a: country_code
    field_b: domicile
    method: exact
    weight: 0.20

  - field_a: lei
    field_b: lei_code
    method: exact
    weight: 0.05

thresholds:
  auto_match: 0.85
  review_floor: 0.60

output:
  results_path:   bench/output/results.csv
  review_path:    bench/output/review.csv
  unmatched_path: bench/output/unmatched.csv

performance:
  encoder_pool_size: 4
